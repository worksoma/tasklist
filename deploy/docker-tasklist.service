[Unit]
Description=Tasklist container
Requires=docker.service
After=docker.service

[Service]
Restart=always
ExecStart=/usr/bin/docker start -a tasklist 
ExecStop=/usr/bin/docker stop -t 2 tasklist

[Install]
WantedBy=default.target

